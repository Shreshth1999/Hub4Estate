version: 1
frontend:
  phases:
    install:
      commands:
        - nvm install 20
        - nvm use 20
        - npm install --ignore-scripts
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
